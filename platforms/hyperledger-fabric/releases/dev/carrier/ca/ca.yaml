apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: ca
  namespace: carrier-net
  annotations:
    fluxcd.io/automated: "false"
spec:
  interval: 1m
  releaseName: ca
  chart:
    spec:
      interval: 1m
      sourceRef:
        kind: GitRepository
        name: flux-develop
        namespace: flux-develop
      chart: platforms/hyperledger-fabric/charts/fabric-ca-server   
  values:
    global:
      serviceAccountName: vault-auth
      cluster:
        provider: minikube
        cloudNativeServices: false
        kubernetesUrl: https://minikube:53079
      vault:
        type: hashicorp
        network: fabric
        address: http://172.20.10.2:8200
        authPath: developcarrier
        secretEngine: secret
        secretPrefix: "data/developcarrier"
        role: vault-role
        tls: false
      proxy:
        provider: none
        externalUrlSuffix: develop.local.com

    storage:
      size: 512Mi
      reclaimPolicy: "Delete"
      volumeBindingMode: Immediate
      allowedTopologies:
        enabled: false

    image:
      alpineUtils: ghcr.io/henjimantaring/bevel-alpine:arm64
      ca: ghcr.io/henjimantaring/bevel-fabric-ca:1.4.8
      pullSecret: regcred

    server:
      removeCertsOnDelete: true
      tlsStatus: true
      adminUsername: carrier-admin
      adminPassword: carrier-adminpw
      subject: "/C=GB/ST=London/L=London/O=Carrier/CN=ca.carrier-net"
      clusterIpPort: 7054

